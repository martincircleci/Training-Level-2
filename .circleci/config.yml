version: 2.1

jobs:
  runner:
    docker:
      - image: circleci/node
    resource_class: martincircleci-namespace/small
    steps:
      - run: echo "Hi I'm on Runners!"

workflows:
  testing:
    jobs:
      - runner
