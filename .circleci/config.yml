version: 2.1

jobs:
  runner:
    machine: true
    resource_class: martincircleci-namespace/small
    steps:
      - run: echo "Hi I'm on Runners!"
    
  runnertwo:
    machine: true
    resource_class: martincircleci-namespace/martin
    steps:
      - run: echo "Runner two"

workflows:
  testing:
    jobs:
      - runner
      - runnertwo
